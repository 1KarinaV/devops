pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Try to deploy to Kubernetes'
            }
        }

        stage('Test') {
            steps {
                echo 'Another steps'
            }
        }
    }
}
